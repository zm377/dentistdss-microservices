# DentistDSS Microservices Environment Configuration
#
# ⚠️  SECURITY WARNING ⚠️
# This is a template file with placeholder values only.
# NEVER commit actual passwords or secrets to version control.
#
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update all placeholder values with your actual credentials
# 3. Ensure .env is in .gitignore (it should be ignored by default)
# 4. Use strong, unique passwords for all services

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL Database
POSTGRES_PASSWORD=your_postgres_password_here
POSTGRES_READONLY_PASSWORD=your_readonly_password_here

# MongoDB Database
MONGO_INITDB_ROOT_PASSWORD=your_mongo_password_here

# Redis Cache
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here

# =============================================================================
# Spring Cloud Configuration
# =============================================================================

# Config Server Authentication
SPRING_CONFIG_USER=config_user
SPRING_CONFIG_PASS=config_password

# Git Repository for Configuration
GIT_REPO=https://github.com/your-username/dentistdss-config.git

# =============================================================================
# Security Configuration
# =============================================================================

# SSL/TLS Certificate Store
CA_STOREPASS=your_keystore_password

# JWT Configuration
JWT_RSA_PRIVATE_KEY=your_rsa_private_key_here
JWT_RSA_PUBLIC_KEY=your_rsa_public_key_here
JWT_RSA_KEY_ID=your_key_id_here

# =============================================================================
# Email Configuration
# =============================================================================

# SMTP Settings
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password

# =============================================================================
# OAuth2 Configuration
# =============================================================================

# Google OAuth2
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# =============================================================================
# AI/ML Configuration
# =============================================================================

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4

# Google Vertex AI Configuration
VERTEX_AI_PROJECT_ID=your_gcp_project_id
VERTEX_AI_LOCATION=us-central1
GEMINI_MODEL=gemini-pro
VERTEX_AI_ENABLED=false

# =============================================================================
# Application URLs
# =============================================================================

# Base URL for email verification and OAuth redirects
BASE_URL=https://dentistdss.com

# Auth Service URL for JWT validation
AUTH_SERVICE_URL=http://localhost:8081
